---
- hosts: etcds
  vars:
    - PROJECT_DIR: "{{ PROJECT_BASED_DIR }}/etcd"
    - CERT_NAME: kubernetes
    - version: 3.2.24
  tasks:
    - import_role:
        name: ca-client
    - import_role:
        name: etcd-docker-compose
